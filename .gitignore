.env
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Build artifacts — rebuilt by `python -m echorag.index`, never committed (AUDIT.md §4)
# Any built index — they are 50-750 MB and live in a HF dataset repo instead
# (scripts/push_index.py). The bare 'index' entry missed index-small/ and
# index-mid/, and a `git add -A` pushed 219 MB before this was widened.
index/
index-*/
data/
bench/runs.jsonl
bench/*.png
.vercel
.env*
