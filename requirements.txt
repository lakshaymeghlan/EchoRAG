# HF Spaces (gradio SDK) installs from this file, not pyproject.toml.

# Default (CUDA) torch, not the CPU-only wheel: the free Gradio tier is
# ZeroGPU-only, and the `spaces` runtime expects a CUDA-capable torch even
# though our request path never leaves the CPU. Locally, use the CPU wheel:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch

# Required by ZeroGPU — see the @spaces.GPU probe in app.py.
spaces

fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9
httpx>=0.27
pydantic>=2.9
python-dotenv>=1.0
lancedb>=0.15
datasets>=3.0
sentence-transformers>=3.0
huggingface_hub>=0.25
